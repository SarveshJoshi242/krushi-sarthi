package com.krushiadhaar.app.ui.theme

import androidx.compose.ui.graphics.Color

// --- Farmer Flow (Green) ---
val GreenPrimary     = Color(0xFF2D7A3A)  // Dark forest green (weather card, buttons)
val GreenDark        = Color(0xFF1E5C2B)  // Darker green for gradient/header
val GreenLight       = Color(0xFF4CAF50)  // Lighter accent green
val GreenSurface     = Color(0xFFE8F5E9)  // Very light green tint (quick action card bg)
val GreenText        = Color(0xFF2D7A3A)  // Green used on text (role name, status)

// --- Buyer / Marketplace Flow (Orange) ---
val OrangePrimary    = Color(0xFFF46C20)  // Figma orange for "Place Order" buttons
val OrangeLight      = Color(0xFFFFF3E0)  // Light orange tint background
val OrangeText       = Color(0xFFF46C20)  // Orange text (prices in marketplace)

// --- Neutral / UI ---
val AppBackground    = Color(0xFFF5F5F5)  // Outer screen background
val AppSurface       = Color(0xFFFFFFFF)  // Cards, sheets
val AppDivider       = Color(0xFFE0E0E0)  // Dividers, borders
val TextPrimary      = Color(0xFF1A1A1A)  // Main body text
val TextSecondary    = Color(0xFF757575)  // Sub-labels, hints
val TextMuted        = Color(0xFFBDBDBD)  // Placeholder text

// --- Status ---
val RedError         = Color(0xFFD32F2F)  // Low stock warning, error states
val RedLight         = Color(0xFFFFEBEE)  // Light red tint background

// --- Retained for Theme compatibility ---
val AppOnPrimary        = Color(0xFFFFFFFF)
val AppOnBackground     = Color(0xFF1B5E20)
val AppSurfaceVariant   = Color(0xFFE8F5E9)
val AppOnSurfaceVariant = Color(0xFF2E7D32)
